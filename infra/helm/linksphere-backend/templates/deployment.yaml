apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "linksphere-backend.fullname" . }}
  labels:
    app: {{ include "linksphere-backend.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "linksphere-backend.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "linksphere-backend.name" . }}
    spec:
      containers:
        - name: {{ include "linksphere-backend.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
