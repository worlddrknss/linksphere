apiVersion: v1
kind: Service
metadata:
  name: {{ include "linksphere-backend.fullname" . }}
  labels:
    app: {{ include "linksphere-backend.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "linksphere-backend.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
