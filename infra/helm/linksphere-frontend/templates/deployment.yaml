apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "linksphere-frontend.fullname" . }}
  labels:
    app: {{ include "linksphere-frontend.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "linksphere-frontend.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "linksphere-frontend.name" . }}
    spec:
      containers:
        - name: {{ include "linksphere-frontend.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
