apiVersion: v1
kind: Service
metadata:
  name: {{ include "linksphere-frontend.fullname" . }}
  labels:
    app: {{ include "linksphere-frontend.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "linksphere-frontend.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
